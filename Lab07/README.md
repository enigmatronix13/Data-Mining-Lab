# Lab 7: K-Means Algorithm

## Objectives

* To implement the K-Means clustering algorithm
* To understand clustering using different similarity measures
* To apply distance metrics for grouping data points

## Lab Exercise

1. Implement K-Means algorithm for a given dataset using Euclidean distance as a similarity measure.

## Additional Exercise

1. Use Manhattan distance as a similarity measure to cluster the given dataset using K-Means algorithm.